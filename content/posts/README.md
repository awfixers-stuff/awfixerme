# Blog posts

Blog content is no longer stored here. Posts are managed in [Marble](https://marblecms.com) and fetched at build time via `@usemarble/sdk` using `MARBLE_API_KEY`.

This folder is kept as a placeholder so existing paths remain obvious; you can remove it if you prefer.
