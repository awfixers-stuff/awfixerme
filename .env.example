# Marble CMS — Settings → API Keys (server-only; never expose to the client)
MARBLE_API_KEY=

# Optional: Marble dashboard → Webhooks → Copy secret → URL: https://your-domain.com/api/revalidate
MARBLE_WEBHOOK_SECRET=
